export interface ImagenDTO {
    id: number;
    denominacion: string; // <--- Esta propiedad contiene la URL de la imagen según tu JSON
    // Añade aquí otras propiedades de Imagen si las necesitas en el frontend y están en el DTO del backend
}