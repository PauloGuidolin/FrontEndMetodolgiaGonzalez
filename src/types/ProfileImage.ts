export interface ProfileImage {
  id?: number; // El ID de la imagen (opcional si es una imagen nueva o no se usa en el frontend)
  url: string; // La URL de la imagen
  name: string; // El nombre del archivo de la imagen
}